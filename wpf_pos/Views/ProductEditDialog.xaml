<Window x:Class="WPF_POS.Views.ProductEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="商品管理" Height="420" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#2D2D30">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="DialogTitle" Text="新增商品" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="商品名稱" Margin="0,0,0,4"/>
            <TextBox x:Name="NameBox" Padding="8,6" FontSize="14" Background="#3E3E42" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="價格" Margin="0,0,0,4"/>
            <TextBox x:Name="PriceBox" Padding="8,6" FontSize="14" Background="#3E3E42" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="種類" Margin="0,0,0,4"/>
            <ComboBox x:Name="KindCombo" Padding="8,6" FontSize="14" Background="#3E3E42" Foreground="White"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,0,0,12">
            <TextBlock Text="標籤（可多選）" Margin="0,0,0,4"/>
            <ListBox x:Name="TagList" SelectionMode="Multiple" Background="#3E3E42" Foreground="White" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" Tag="{Binding Id}" Margin="0,4"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="80" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="儲存" Width="80" Style="{StaticResource PrimaryButton}" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>