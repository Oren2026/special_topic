<UserControl x:Class="WPF_POS.Views.ProductManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 標題 + 新增 -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📦 商品管理" FontSize="20" FontWeight="Bold"/>
            <Button Content="+ 新增商品" HorizontalAlignment="Right"
                    Style="{StaticResource PrimaryButton}"
                    Click="AddProduct_Click"/>
        </Grid>

        <!-- 搜尋列 -->
        <TextBox Grid.Row="1"
                 Text="{Binding ProductSearchText, UpdateSourceTrigger=PropertyChanged}"
                 Padding="10,8" FontSize="14" Margin="0,0,0,16"
                 Background="#3E3E42" Foreground="White"
                 BorderThickness="0"/>

        <!-- 商品列表 -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredProducts}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Background="Transparent"
                  RowBackground="#3E3E42"
                  AlternatingRowBackground="#4E4E52"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  BorderThickness="0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="名稱" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="價格" Binding="{Binding Price, StringFormat=N0}" Width="80"/>
                <DataGridTextColumn Header="種類" Binding="{Binding KindId}" Width="80"/>
                <DataGridTextColumn Header="標籤" Binding="{Binding TagIds}" Width="100"/>
                <DataGridTemplateColumn Header="操作" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="編輯" Margin="0,0,8,0" Click="EditProduct_Click" Tag="{Binding}"/>
                                <Button Content="刪除" Style="{StaticResource DangerButton}" Click="DeleteProduct_Click" Tag="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>