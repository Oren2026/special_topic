<UserControl x:Class="WPF_POS.Views.KindManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📁 種類管理" FontSize="20" FontWeight="Bold"/>
            <Button Content="+ 新增種類" HorizontalAlignment="Right"
                    Style="{StaticResource PrimaryButton}"
                    Click="AddKind_Click"/>
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredKinds}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Background="Transparent"
                  RowBackground="#3E3E42"
                  AlternatingRowBackground="#4E4E52"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  BorderThickness="0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="名稱" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="排序" Binding="{Binding DisplayOrder}" Width="80"/>
                <DataGridTemplateColumn Header="操作" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="編輯" Margin="0,0,8,0" Click="EditKind_Click" Tag="{Binding}"/>
                                <Button Content="刪除" Style="{StaticResource DangerButton}" Click="DeleteKind_Click" Tag="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>