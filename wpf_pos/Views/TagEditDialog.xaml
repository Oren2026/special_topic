<Window x:Class="WPF_POS.Views.TagEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="標籤管理" Height="220" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#2D2D30">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="新增標籤" x:Name="DialogTitle" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="標籤名稱" Margin="0,0,0,4"/>
            <TextBox x:Name="NameBox" Padding="8,6" FontSize="14" Background="#3E3E42" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="顏色（HEX）" Margin="0,0,0,4"/>
            <TextBox x:Name="ColorBox" Padding="8,6" FontSize="14" Background="#3E3E42" Foreground="White" BorderThickness="0" Text="#4CAF50"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="80" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="儲存" Width="80" Style="{StaticResource PrimaryButton}" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>