"""
lib/vision/params.py
視覺相關物理常數（從 windows/config.py 提取）

這些是純數值，不涉及任何硬體或 UI 框架。
"""

# =============================================================================
# 球檯幾何（與 lib/sim_table、lib/physics/parameters 共享）
# =============================================================================
TABLE_WIDTH  = 1200   # mm（含庫邊）
TABLE_HEIGHT = 630    # mm（含庫邊）

# =============================================================================
# 球 / 口袋
# =============================================================================
BALL_DIAMETER   = 38.0   # mm
POCKET_DIAMETER = 50.0   # mm（洞口直徑）
BALL_RADIUS     = BALL_DIAMETER / 2   # 19.0 mm
POCKET_RADIUS   = POCKET_DIAMETER / 2  # 25.0 mm

# =============================================================================
# 攝影機
# =============================================================================
CAM_TOP_ID   = 1        # 頂視鏡頭 VideoCapture ID
CAM_SIDE_ID  = 2        # 側視鏡頭 VideoCapture ID
FRAME_WIDTH  = 640
FRAME_HEIGHT = 480
CAMERA_FPS   = 15
FOURCC       = "MJPG"   # MJPEG 壓縮（4 char）

# =============================================================================
# 球檯畫布（Top view，pixels）
# =============================================================================
TOP_CANVAS_W = 800
TOP_CANVAS_H = 600

# =============================================================================
# 口袋顏色定義（cv2 BGR）
# =============================================================================
BALL_COLORS = {
    "CUE_BALL":     (255, 255, 255),   # 白
    "TARGET_BALL":  (0,   255,   0),   # 綠
    "POCKET":       (255, 255,   0),   # 青藍
}
DEFAULT_BALL_COLOR = (0, 0, 255)        # 紅（未知類型）