"""
config.py — HIWIN 系統所有參數集中管理
所有模組統一從此處讀取參數，勿自行 hardcode
"""

import os

# =============================================================================
# 硬體參數（球檯）
# =============================================================================
TABLE_WIDTH  = 1200   # mm
TABLE_HEIGHT = 630    # mm

# =============================================================================
# 硬體參數（球）
# =============================================================================
BALL_DIAMETER   = 38.0   # mm
POCKET_DIAMETER = 50.0   # mm（洞口直徑）

# =============================================================================
# 硬體參數（擊球機構）
# =============================================================================
ACCEL_DIST_LIMIT = 150.0   # 最大加速行程 mm
FORCE_FACTOR     = 1.0      # 力道係數（預留）
FIXED_LENGTH     = 200.0   # TCP 到桿頭縮回時的固定距離 mm
SAFE_GAP         = 80.0    # 桿頭與白球的初始安全距離 mm
MAX_STROKE       = 200.0   # 擊球機構最大行程 mm

# =============================================================================
# 硬體參數（手臂）
# =============================================================================
ROBOT_MAX_REACH  = 750     # 手臂最大可達半徑 mm
ROBOT_OFFSET_X   = 600     # 手臂原點在 X 軸偏移量（長邊中心對齊）
ROBOT_OFFSET_Y   = 0       # 手臂原點在 Y 軸偏移量

# =============================================================================
# 視覺參數
# =============================================================================
CAM_TOP_ID  = 1    # 頂視鏡頭 VideoCapture ID
CAM_SIDE_ID = 2    # 側視鏡頭 VideoCapture ID
FRAME_WIDTH  = 640
FRAME_HEIGHT = 480
CAMERA_FPS   = 15

# MJPG 壓縮（4 char，釋放 USB 頻寬）
FOURCC = "MJPG"

# =============================================================================
# 網路參數（由 protocol.py 提供，此處同步）
# =============================================================================
SOCKET_HOST = "127.0.0.1"
SOCKET_PORT = 5005

# =============================================================================
# UI 參數
# =============================================================================
TOP_CANVAS_W = 800
TOP_CANVAS_H = 600
SIDE_CANVAS_W = 400
SIDE_CANVAS_H = 300
UI_UPDATE_MS  = 15   # Tkinter after 間隔
