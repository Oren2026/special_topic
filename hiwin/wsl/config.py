# =============================================================================
# 球檯
# =============================================================================
TABLE_WIDTH  = 1200   # mm
TABLE_HEIGHT = 630    # mm

# =============================================================================
# 球
# =============================================================================
BALL_DIAMETER    = 38.0   # mm
POCKET_DIAMETER  = 45.0   # mm

# =============================================================================
# 手臂（直接控制，座標送至上銀）
# =============================================================================
ROBOT_OFFSET_X = 600      # 手臂原點 X 偏移（長邊中心）
ROBOT_OFFSET_Y = 0        # 手臂原點 Y 偏移
ROBOT_MAX_REACH = 750     # 最大可達半徑 mm

# =============================================================================
# 擊球機構（Arduino USB Serial）
# =============================================================================
# ⚠️ 尚未連線，預留值，待硬體確認後調整
ARDUINO_DEVICE   = "/dev/ttyUSB0"   # 裝置名稱（/dev/ttyUSB0 / ttyACM0）
ARDUINO_BAUD    = 115200            # 鮑率（9600 / 115200）
ARDUINO_TIMEOUT = 1.0               # 序列逾時秒數

# 測試模式：True = 只列印指令，不實際發送至 Arduino
STRIKER_MOCK_MODE = True

# =============================================================================
# 擊球機構參數
# =============================================================================
FIXED_LENGTH  = 200.0   # TCP 到桿頭縮回時的固定距離 mm
SAFE_GAP      = 80.0    # 桿頭與白球的初始安全距離 mm
MAX_STROKE    = 200.0   # 最大擊球行程 mm

# =============================================================================
# 網路
# =============================================================================
SOCKET_HOST = "0.0.0.0"
SOCKET_PORT = 5005
